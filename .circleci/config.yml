version: 2.1
jobs:
  build:
    working_directory: /go/src/github.com/Siyavashshams/invoicer-chapter2
    docker:
      - image: cimg/go:1.19.0
        auth:
          username: "siyavashshams"  # can specify string literal values
          password: "lasTampa34?m"  # or project environment variable 
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: The First Step
          command: |
            echo 'Hello Worldddddddddddd!'
            echo 'This is the delivery pipeline'